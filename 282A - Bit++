#include <iostream>

using namespace std;

int main()
{
    int n,i=0,x=0;
    string in;
    cin>>n;

    while(i<n){
        cin>>in;

        if(in=="X++" || in=="++X"){
            x = x+1;
        }
        else{
            x = x-1;
        }

        i++;
    }

    cout<<x;
    return 0;
}
